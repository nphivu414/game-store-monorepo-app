type Game {
    id: Int!
    name: String
    backgroundImage: String
    rating: Float,
    platforms: [Platform]
}

type PlatformDetails {
    id: Int!,
    name: String,
    slug: String,
    image: String,
    yearEnd: Int,
    yearStart: Int,
    gamesCount: Int,
    imageBackground: String
}

type Platform {
  platform: PlatformDetails,
  releasedAt: String
}

type Query {
    allGames(
        page: Int,
        pageSize: Int,
        search: String,
        genres: String,
        tags: String,
        dates: String
    ): [Game]
}
